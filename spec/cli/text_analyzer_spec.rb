require 'spec_helper'

describe "bin/text_analyzer" do
  it "accepts arguments as a list of one or more file paths"
  it "accepts input on stdin"
  it "displays help text if no input is provided"
end
